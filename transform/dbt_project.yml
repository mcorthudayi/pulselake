name: pulselake
version: "1.0.0"
config-version: 2
profile: pulselake

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  pulselake:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
    deid:
      +schema: deid
      +materialized: table
